def diff():
    actual = int(input("Enter the actual length: "))
    estimate = int(input("Enter the estimated length: "))

    difference = actual - estimate

    print("Difference between actual & estimated is",difference)

diff()